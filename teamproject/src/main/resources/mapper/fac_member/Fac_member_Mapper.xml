<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.teamproject.mapper.Fac_member_Mapper">
	
	<!-- 회원 가입 -->
	<insert id="insertMemberList">
		<selectKey resultType="int" keyProperty="m_no" order="BEFORE">
			select member3_seq.nextval from dual
		</selectKey>
		insert into member3 (m_no,m_id,m_pw,m_name,m_nickname,m_email)
		values(#{m_no},#{m_id},#{m_pw},#{m_name},#{m_nickname},#{m_email})
	</insert>
	
	<!--회원 로그인 -->
	<select id="memberLogin" resultType="com.example.teamproject.vo.Fac_member_Vo">
		select * from member3 where m_id = #{m_id} and m_pw =#{m_pw}
	</select>
	
</mapper>